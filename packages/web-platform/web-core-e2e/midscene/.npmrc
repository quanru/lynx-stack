# Public npm registry so installs work on any network (CI runners included);
# the committed package-lock.json pins every resolved URL here too and verifies
# tarballs against its sha512 integrity hashes.
registry=https://registry.npmjs.org/
fund=false
audit=false
